$(document).ready(function(){
    $("h2").css("text-decoration", "underline"); //underlines all <h2> elements
    // $("h2").addClass("underline"); adds the already existing css class to elements
    // $("h2").removeClass("underline"); remove the already existing css class to elements
    $("ul").css("border", "solid, 1px, #ccc") //adds a border to all lists
    // $("ul").addClass("border"); adds the border class from css to elements
    // $("ul").removeClass("border"); remove the border class from css to elements
})